DATABASE_URL="postgres://postgres:@127.0.0.1:5432/shadow_dev"

PINECONE_API_KEY=
PINECONE_INDEX_NAME="shadow"

# OAuth app credentials (required for user login)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Personal token for local dev (optional)
# If set (and NODE_ENV!=production), server will use this when no user token exists
GITHUB_PERSONAL_ACCESS_TOKEN=
GITHUB_TOKEN=

# Local workspace directory (used in AGENT_MODE=local)
WORKSPACE_DIR=

# Socket.IO settings (ms)
SOCKET_PING_TIMEOUT_MS=30000
SOCKET_PING_INTERVAL_MS=25000
